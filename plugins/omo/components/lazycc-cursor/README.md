# lazycc-cursor

Codex plugin-internal LazyCC Cursor bridge helper. This component keeps the
orchestration surface inside the installed Codex plugin while reusing the
vendored `packages/cursor-ai-proxy-bridge` runtime.
